#include <iostream>
using namespace std;

int main()
{
  int total = 0, angka, a = 1;
  
  while (a <= 10){
    angka = (2*a)-1;
    total += angka; 
    cout << angka; 
    a++;
    if (angka <((2*10)-1))
    cout << " + "; 
  }
  cout << " = " << total; 
}
